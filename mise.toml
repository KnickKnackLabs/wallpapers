# Mise configuration for wallpaper generator
# Tasks are auto-discovered from .mise/tasks/

[tools]
gum = "latest"
jq = "latest"
usage = "latest"  # enables shell completions for task args

# Note: Swift is built-in on macOS, no need to manage it
